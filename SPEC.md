# 仕様書 - タスク帳（Task Notebook）

このドキュメントは「何を作るか」「どう動くべきか」をまとめたものです。
コードを読む前にここを読むと、全体像がつかみやすくなります。

## 1. 概要

React + Vite で作る、ブラウザだけで動くシンプルなタスク管理アプリです。
サーバーやデータベースは使わず、ブラウザの `localStorage`（ブラウザ内の小さな保存領域）に
タスクを保存します。ページを閉じてもタスクは消えません。

見た目は「ノート」をモチーフにしています。タスクを完了すると、
ノートに判子（はんこ）を押したような演出が入ります。

## 2. 目的・対象者

- Reactを学び始めた人が、動くアプリを触りながら仕組みを理解できること
- 過度に複雑な機能は入れず、コード量を最小限に保つこと

## 3. 機能一覧

| # | 機能 | 説明 |
|---|------|------|
| 1 | タスクの追加 | 入力欄にテキストを入れて「追加」ボタンまたは Enter キーで追加する |
| 2 | タスクの完了/未完了の切り替え | チェックボックス（判子）をクリックすると完了⇔未完了が切り替わる |
| 3 | タスクの削除 | 各タスクの「消す」ボタンで削除する |
| 4 | フィルター表示 | 「すべて」「未完了」「完了」の3つの表示切り替えができる |
| 5 | 件数表示 | 現在の未完了タスクの件数を表示する |
| 6 | データの保存 | タスクは `localStorage` に自動保存され、次回アクセス時も残る |

## 4. 画面構成

```
┌─────────────────────────────────────┐
│  タスク帳                              │  ← タイトル（見出し）
│  今日やることを書き留めよう              │  ← サブタイトル
├─────────────────────────────────────┤
│  [ 入力欄..................... ] [追加] │  ← タスク追加フォーム
├─────────────────────────────────────┤
│  [すべて] [未完了] [完了]   残り 2件     │  ← フィルタータブ + 件数
├─────────────────────────────────────┤
│  ( ) 牛乳を買う                  [消す] │
│  (済) ~~レポートを出す~~          [消す] │  ← 完了タスクは打ち消し線+判子
│  ( ) 部屋の掃除                  [消す] │
└─────────────────────────────────────┘
```

## 5. データの形

1件のタスクは、以下のような形（JavaScriptのオブジェクト）で表します。

```js
{
  id: 1699999999999,      // 作成した時刻（ミリ秒）をIDとして使う。重複しにくいので簡単なID代わりになる
  text: "牛乳を買う",       // タスクの内容
  done: false,             // 完了しているかどうか（true/false）
  createdAt: "2026-08-09T10:00:00.000Z" // 作成日時（表示はしないが将来の拡張用）
}
```

タスク全体は、この形のオブジェクトを集めた「配列（リスト）」として管理します。

```js
[
  { id: 1, text: "牛乳を買う", done: false, createdAt: "..." },
  { id: 2, text: "レポートを出す", done: true,  createdAt: "..." },
]
```

## 6. 画面遷移・状態

このアプリは画面遷移（ページ移動）がない、1画面だけのアプリです。
そのかわり、以下の「状態（state）」がアプリの中で切り替わります。

- `tasks`：タスクの配列（追加・削除・完了切り替えでこの中身が変わる）
- `filter`：今どの表示モードか（`"all"` / `"active"` / `"done"`）
- 入力欄の文字列（フォームに何を打っているか）

## 7. 使用技術

| 項目 | 使用するもの | 補足 |
|------|--------------|------|
| フレームワーク | React 18 | 画面部品（コンポーネント）を組み立てて画面を作る |
| ビルドツール | Vite | 開発サーバーの起動やビルドを高速に行うツール |
| 言語 | JavaScript (JSX) | ロジックとHTMLのような構造を1つのファイルに書ける |
| スタイル | 素のCSS（App.css / index.css） | 外部UIライブラリは使わず、CSSの基本を学べるようにする |
| データ保存 | ブラウザの localStorage | サーバー不要。ブラウザ内に保存される |

外部の状態管理ライブラリ（Reduxなど）やUIキットは使わず、
Reactの基本機能（`useState` と `useEffect`）だけで作っています。
これは「Reactの基礎を理解する」という目的に合わせた設計判断です。

## 8. ファイル構成

```
task-manager-app/
├── README.md          … セットアップ手順（初心者向け）
├── SPEC.md            … このファイル（仕様書）
├── package.json        … 使用するパッケージや起動コマンドの定義
├── vite.config.js       … Viteの設定ファイル
├── index.html           … アプリの土台となるHTML
└── src/
    ├── main.jsx                 … アプリの起動地点
    ├── App.jsx                  … 全体の状態管理と画面の組み立て
    ├── App.css                  … 見た目のスタイル
    ├── index.css                … 全体の基本スタイル
    └── components/
        ├── TaskForm.jsx          … タスク追加フォーム（部品）
        ├── TaskList.jsx          … タスク一覧の表示（部品）
        └── TaskItem.jsx          … タスク1件分の表示（部品）
```

## 9. 今後拡張するとしたら（対象外・任意）

以下は今回の実装には含まれていませんが、学習の次のステップとして良い題材です。

- タスクの編集機能（テキストの書き換え）
- 期限日の設定と並び替え
- 複数リスト（カテゴリ）対応
- サーバー保存（Firebase や自作APIとの連携）

このSPEC.mdは「仕様書ページ」としてアプリ内からも見られるようにしています
（画面下部の「仕様書を見る」リンク、またはアプリ内のモーダル表示）。
